import 'package:flutter/material.dart';

class ReservationPage extends StatelessWidget {
  const ReservationPage({super.key});

  @override
  Widget build(BuildContext context) {
    print('reserve');
    return Scaffold(body: Center(child: Text('ReservationPage')));
  }
}
